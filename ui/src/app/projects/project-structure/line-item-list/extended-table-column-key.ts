/**
 * Describes the keys to be used to access extended attribute data
 * that is supposed to show up as table columns
 */
export enum ExtendedTableColumnKey {
  rASessionId = 'RASessionId',
  evidenceDownloadStatus = 'Evidence Download Status',
  sharedId = 'SharedId',
  section = 'Section',
  rASessionRecordId = 'RASessionSystemRecordId'
}
